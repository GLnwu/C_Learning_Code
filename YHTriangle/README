Wed Mar 26,1:04 PM
	昨天晚上写完了杨辉三角的输出，但是是依靠书做的，YHtriangle.c and YHTirangle.h
	今天早上又在不看书的情况下，重做了一边 YHtriangle1.c
	杨辉三角的输出关键在于：
		1.利用循环队列的特点，在输出front指针指向的元素后，随即进行入队列操作插入到rear指针指向的数组位置；
		即当出第n-1行时，入第n行；
		2.还有就是控制使得输出的杨辉三角是对齐的：
		对于每一行的数字，有1位数，2位的，3位的，如果都使用同样的一个空格来间隔，则会使得杨辉三角无法对齐，如
				1
			       1  1
		              1 2 1
			     1 3 3 1
		而如果使用多个空格，也有与100和96如果时杨辉三角的相邻数字，会使数字无法对其，因为100占据了三个字符，无形中等价与多输出了一个空格。
		所以对于不同的位数的数，要分情况输出，1位输5位空格的话，2位就输出4位空格，依次类推。。。
	
